# Settle frontend

Next.js (App Router) + TypeScript + Tailwind.

## Setup

```bash
npm install
npm run dev
```

App: http://localhost:3000

## Structure

See `docs/PRD.md` (Frontend Architecture). Feature ownership in `docs/TEAM_AGREEMENT.md`.
