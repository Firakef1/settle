# Frontend team agreement

- Dev 1 owns: `features/auth`, `features/requests`, auth + request pages
- Dev 2 owns: `features/approvals`, `features/admin`, dashboard + settings pages
- Changes under `shared/` require a short discussion first
- API shapes live in `docs/API_CONTRACT.md` and should match the Go backend
